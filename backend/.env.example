# Rename this file to .env for local development only.
PORT=4000
APP_NAME=WhatsApp Sales Tracker API
LOG_LEVEL=info

# Connection string (use for hosted providers like Supabase)
DATABASE_URL=
DB_PROVIDER=local
DB_MAX_CONNECTIONS=5
DB_HOST=localhost
DB_PORT=5432

# Local Postgres credentials (used when DATABASE_URL is empty)
DB_NAME=
DB_USER=
DB_PASSWORD=
